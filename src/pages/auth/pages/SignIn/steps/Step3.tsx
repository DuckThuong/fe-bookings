import type { FormInstance } from "antd";
import "../style.scss";
interface SignInProps {
  form: FormInstance;
}

export const Step3 = (props: SignInProps) => {
  return <div className="signIn__step-3"></div>;
};
